package com.example.forwarder;

// DELETED: Legacy ConfirmationController
// Confirmations are now handled via HTTP response codes in SendService
// The DeliveryResultHandler processes responses asynchronously in batches
// No need for separate /confirm endpoint anymore

